/**
 * Domain events barrel export.
 *
 * KC-020 execution lifecycle events are defined in `@/execution`
 * (ExecutionStarted, ExecutionCompleted, NextBestActionGenerated, …).
 * Keep domain free of framework dependencies; import from `@/execution` directly.
 */

export {}
