/**
 * KC-037B — Built-in report configuration presets.
 * KC-037C-F: re-export template catalog (single source).
 */

export {
  BUILTIN_TEMPLATES as REPORT_PRESET_CATALOG,
  getReportPreset,
  listEnabledReportPresets,
  listReportPresets,
} from './templates/reportTemplates'
