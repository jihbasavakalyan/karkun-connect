# Authentication Workflows

Multi-step authentication processes for **karkun-connect**.

Orchestrates sign-in, registration, password reset, email verification, and session refresh by coordinating the `authentication` module, Firebase, and notification workflows.

Examples: `signInWorkflow`, `registerWorkflow`, `resetPasswordWorkflow`.
