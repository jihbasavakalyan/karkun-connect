# Reporting Workflows

Report generation and delivery flows for **karkun-connect**.

Orchestrates parameter validation, data aggregation, report rendering, and export/delivery by coordinating the `reporting` and `notifications` modules.

Examples: `generateReportWorkflow`, `scheduleReportWorkflow`, `exportReportWorkflow`.
