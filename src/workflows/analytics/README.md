# Analytics Workflows

Metric collection and aggregation pipelines for **karkun-connect**.

Orchestrates event ingestion, batch aggregation, snapshot generation, and cache refresh by coordinating the `analytics` module and related services.

Examples: `trackEventWorkflow`, `refreshMetricsWorkflow`, `generateSnapshotWorkflow`.
