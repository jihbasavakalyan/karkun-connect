# Jih Workflows

Jih-specific multi-step processes for **karkun-connect**.

Orchestrates Jih entity lifecycle, status transitions, and cross-module operations by coordinating the `jih` module with assignments, visits, and notifications.

Examples: `createJihWorkflow`, `transitionJihStatusWorkflow`.
