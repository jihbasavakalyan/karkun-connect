# Notifications Workflows

Notification dispatch and preference flows for **karkun-connect**.

Orchestrates preference resolution, message composition, multi-channel dispatch, and delivery confirmation by coordinating the `notifications` module.

Examples: `sendNotificationWorkflow`, `updatePreferencesWorkflow`, `markAllReadWorkflow`.
