# Visits Workflows

Visit scheduling and completion pipelines for **karkun-connect**.

Orchestrates visit creation, rescheduling, check-in/out, and post-visit follow-up by coordinating the `visits`, `assignment`, and `notifications` modules.

Examples: `scheduleVisitWorkflow`, `completeVisitWorkflow`, `cancelVisitWorkflow`.
