# Assignment Workflows

End-to-end assignment lifecycle processes for **karkun-connect**.

Orchestrates creating assignments, notifying assignees, updating status, and closing assignments by coordinating the `assignment` and `notifications` modules.

Examples: `createAssignmentWorkflow`, `reassignWorkflow`, `closeAssignmentWorkflow`.
