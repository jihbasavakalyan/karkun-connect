# Automation Workflows

Trigger evaluation and action execution flows for **karkun-connect**.

Orchestrates rule evaluation, condition matching, action dispatch, and retry handling by coordinating the `automation` module with other domain modules.

Examples: `evaluateTriggerWorkflow`, `executeAutomationWorkflow`.
