# Constants

Application-wide constant values for **karkun-connect**.

Immutable values referenced throughout the codebase.

Examples: route paths, API endpoints, role identifiers, status codes, configuration flags.

**Guidelines:**

- Use `UPPER_SNAKE_CASE` for primitive constants
- Group related constants into named objects or enums
- Never hardcode magic strings in components — reference constants from here
