# Shared Pages

Cross-role pages accessible to multiple user types.

Pages in this folder are not tied to a single role. Examples: user profile, account settings, help/support, and error pages (404, 403).

Shared pages should avoid role-specific logic; use hooks or context for conditional rendering when needed.
