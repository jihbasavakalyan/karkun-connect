# Rukn Pages

Rukn (member) portal pages for **karkun-connect**.

Pages in this folder serve the primary end-user audience. Examples: member dashboard, community features, notifications, and member-specific workflows.

Business logic should be delegated to `src/services/` and shared components to `src/components/`.
