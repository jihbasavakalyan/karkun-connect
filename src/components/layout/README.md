# Layout Components

Structural UI components that define page regions and navigation chrome.

Examples: app header, sidebar navigation, breadcrumb trails, page containers, and responsive grid wrappers.

These components compose the visual shell around page content and work with `src/layouts/` for full-page layouts.
