# Dashboard Components

Dashboard-specific widgets and data display components.

Examples: stat cards, charts, activity feeds, summary tables, and dashboard-specific filters.

Keep dashboard logic encapsulated here rather than in page components to enable reuse across admin and rukn views.
