# Routes

Route definitions and navigation configuration for **karkun-connect**.

This folder centralizes all application routing logic:

- Route path constants
- Route-to-page component mappings
- Nested route configurations
- Route guards (authentication, role-based access)
- Lazy-loaded route modules

Keeping routes separate from pages enables clear navigation structure and easier testing.
