# Firebase

Firebase SDK setup and configuration for **karkun-connect**.

This folder contains all Firebase-related initialization and helpers:

- Firebase app initialization
- Authentication instance and helpers
- Firestore/Realtime Database references
- Cloud Storage configuration
- Environment-specific Firebase config

Keep Firebase SDK calls centralized here rather than scattered across components or services.
