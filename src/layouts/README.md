# Layouts

Page shell and wrapper layout components for **karkun-connect**.

Layouts define the outer structure shared by groups of pages — navigation, sidebars, and content areas. They wrap page components via the router.

Examples: `AdminLayout`, `RuknLayout`, `AuthLayout`, `PublicLayout`.

Layouts compose structural components from `src/components/layout/` and are referenced in `src/routes/`.
