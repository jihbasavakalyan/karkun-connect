# Reporting Module

Report generation and export for **karkun-connect**.

This module owns:

- Report definitions and parameter schemas
- Data aggregation for report output
- Export formats (PDF, CSV, etc.) orchestration
- Scheduled and on-demand report execution

Rendering and download UI lives in pages/components; this module owns report logic and data assembly.
