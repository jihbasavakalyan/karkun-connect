# Notifications Module

Notification delivery and preferences for **karkun-connect**.

This module owns:

- In-app notification creation and read state
- User notification preferences and opt-in rules
- Push/email/SMS dispatch orchestration (when integrated)
- Notification batching and deduplication logic

Notification UI components live in `src/components/`; this module owns delivery and preference management.
